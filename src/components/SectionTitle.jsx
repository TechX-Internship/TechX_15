import React from 'react'

const SectionTitle = ({title}) => {
    return (
        <h6 className='outfit mb-6 text-2xl color-primary'>{title.toUpperCase()}</h6>   
    )
}

export default SectionTitle